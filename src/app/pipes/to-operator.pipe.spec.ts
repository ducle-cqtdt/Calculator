import { ToOperatorPipe } from './to-operator.pipe';

describe('ToOperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ToOperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
